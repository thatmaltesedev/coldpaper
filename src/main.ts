import './style.css';

// Placeholder shell — the full UI lands with the app phase.
const app = document.querySelector<HTMLDivElement>('#app')!;
app.innerHTML = `<main><h1>coldpaper</h1><p>building…</p></main>`;
